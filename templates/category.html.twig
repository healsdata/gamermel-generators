{% extends "base.html.twig" %}

{% block title %}{{ generator.name }} | Generators{% endblock %}

{% block content %}
    <header>
        <figure><img src="/images/fad3.png"></figure>
        <h1>{{ generator.name }}</h1>
    </header>
    <section class="buttons">
        {% for category in categories %}
            <a href="{{ url_for('monster', { 'generator': generator.slug, 'category': category.slug }) }}" class="button">{{ category.name }}</a>
        {% else %}
            <em>No categories found</em>
        {% endfor %}
        <a href="{{ url_for('generator') }}" class="button back">Back</a>
    </section>
{% endblock %}